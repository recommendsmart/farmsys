--- search_api.views_hooks.inc
+++ search_api.views_hooks.inc
@@ -240,7 +240,7 @@ function _search_api_views_get_handlers(FieldInterface $field) {
       $types[] = 'language';
     }
 
-    if ($definition?->getSetting('allowed_values')) {
+    if ($definition?->getSetting('allowed_values') || $definition?->getSetting('allowed_values_function')) {
       $types[] = 'options';
     }
 
